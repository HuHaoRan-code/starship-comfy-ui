# 开发规范与原则

本项目是在 ComfyUI 基础上进行的二次开发项目。为了确保项目的稳定性和可维护性，所有开发活动必须严格遵守以下原则：

## 1. 核心约束：不得修改原生逻辑
- **业务逻辑封锁**：严禁直接修改 ComfyUI 原有的核心业务逻辑、执行流程、节点系统基础架构以及底层核心代码。
- **扩展优于修改**：所有新功能开发必须采用“扩展”模式。通过自定义节点、中间件、API 包装或装饰器模式来实现需求，保持对 ComfyUI 原生代码的零侵入。
- **兼容性保障**：在进行扩展开发时，必须确保其兼容 ComfyUI 的最新版本或项目指定的稳定版本，不得因扩展导致原有系统功能异常。

## 2. 开发流程规范
- **Research -> Strategy -> Execution**：遵循标准的生命周期。在实施任何扩展之前，必须先进行充分的调研，确保方案不触碰原生逻辑。
- **测试驱动**：新增功能必须附带相应的单元测试或集成测试，验证其在不影响原有功能前提下的有效性。
- **代码风格**：严格遵循 ComfyUI 的原有代码风格和 Python 编码规范。

## 3. 版本控制与集成
- **最小化变更**：保持本项目代码与 ComfyUI 上游仓库的同步性，尽量减少对上游文件的直接修改（如果必须修改，请务必建立明确的迁移/补丁说明）。
- **提交规范**：提交信息必须清晰描述变更内容及其对系统扩展性的影响。

## 4. 网络环境配置 (VPN)
在运行依赖网络请求的 Shell 命令（如 `pip install` 或运行需要联网的扩展）之前，如果需要通过 VPN 连接，请先在 Shell 中执行以下命令配置代理：

```bash
export https_proxy=http://127.0.0.1:8118
export http_proxy=http://127.0.0.1:8118
```
